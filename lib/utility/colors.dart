import 'package:flutter/material.dart';

class AppColors {
  static Color primary = Color(
    0xFF0A192F,
  ); //most time use in app is called primary
  static Color secondary = Colors.orange; // second most time color use in app
  static Color background = Colors.pink;
  static Color text = Colors.black;
  static Color textfield = Colors.grey;
}
